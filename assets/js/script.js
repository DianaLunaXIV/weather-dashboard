//Generate required unix time for API call
const generateDateTime = Date.now();

//To use the OneCall API, which provides the most information in the fewest API calls, 
//the user's input must be filtered and converted to lat/long.
function convertFromName(searchTerm) {

};